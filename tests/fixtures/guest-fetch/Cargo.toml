# Test-only guest exercising the http_fetch extension.
[package]
name = "guest-fetch"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
